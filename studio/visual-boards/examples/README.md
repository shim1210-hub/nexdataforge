# Example Gallery

공통 보드가 실제 프로젝트에서 어떻게 적용되는지 모아보는 갤러리다. 현재는 이미지가 없으므로 프로젝트별 참조 구조만 관리한다.

## 예시 구조

- `bidme/`: 요청·제안·예약 경험
- `pricego/`: 가격·혜택·비교 경험
- `soolmap/`: 탐색·지도·장소 경험
- `shimpm/`: 운영·관리·상태 경험

실제 예시는 `{project}-{screen}-{board}-v{version}` 규칙으로 추가하고, 프로젝트 README와 사용한 공통 보드를 함께 연결한다.
