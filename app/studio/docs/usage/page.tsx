import { CodeExample, DocsLayout, DocsSection, GuidelineList } from "../_components/DocsBoard";
export default function Usage() { return <DocsLayout eyebrow="USAGE GUIDE" title="Choose the smallest useful layer" summary="Use the existing system before introducing a new abstraction or visual value."><DocsSection title="Foundation usage"><GuidelineList items={["Prefer semantic tokens over raw values", "Add a component token only for a repeated component decision", "Keep theme changes in semantic tokens", "Check the existing scale before adding a new value"]} /></DocsSection><DocsSection title="Component, pattern, and template usage"><GuidelineList items={["Confirm an existing Component before creating one", "Use Patterns when a flow combines multiple states", "Use Templates for page structure, not finished product screens", "Run responsive, accessibility, and Quality checks before adoption"]} /><CodeExample /></DocsSection></DocsLayout>; }
