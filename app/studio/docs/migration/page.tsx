import { DocsLayout, DocsSection, GuidelineList } from "../_components/DocsBoard";
export default function Migration() { return <DocsLayout eyebrow="MIGRATION GUIDE" title="Migrate without changing behavior" summary="Separate functional protection from visual alignment and work in reviewable slices."><DocsSection title="Migration steps"><GuidelineList items={["Inventory · Button, Input, Card, Navigation, Dialog, Loading, Empty, Error, Table", "Map · match existing UI to Component, Pattern, or Template", "Align foundations · tokens, typography, spacing, radius, shadow", "Replace incrementally · keep existing behavior and routes", "Review responsive and accessibility behavior", "Run regression and define rollback"]} /></DocsSection><DocsSection title="Rollback triggers"><GuidelineList items={["Protected action no longer works", "Mobile layout breaks", "Accessibility gets worse", "Build or TypeScript fails", "Existing route behavior changes"]} /></DocsSection></DocsLayout>; }
