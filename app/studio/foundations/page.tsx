import Link from "next/link";
const foundations = [["Tokens", "Raw, semantic, and component token documentation", "/studio/foundations/tokens"], ["Themes", "Contained light, dark, and high-contrast previews", "/studio/foundations/themes"], ["Colors", "Existing color language reference", "/studio/visual-boards/colors"], ["Typography", "Existing type scale and hierarchy", "/studio/visual-boards/typography"], ["Spacing", "Shared layout and spacing language", ""], ["Accessibility", "Contrast, focus, and touch target guidance", ""]];
export default function FoundationsPage() { return <main className="studio-page"><div className="studio-shell"><Link href="/studio">← Studio</Link><p className="studio-eyebrow">FOUNDATION SYSTEM</p><h1>Design foundations for consistent product work.</h1><p className="studio-lede">Tokens and themes establish shared decisions while existing visual boards remain available for detailed review.</p><div className="studio-board-grid">{foundations.map(([title, description, href], index) => <article className="studio-board-card" key={title}><span className="studio-card-index">0{index + 1} · {href ? "Available" : "Planned"}</span><h2>{title}</h2><p>{description}</p>{href ? <Link className="studio-card-arrow" href={href}>Open foundation →</Link> : <span className="studio-note">Planned foundation</span>}</article>)}</div></div></main>; }
