import { BoardFrame } from "../_components/BoardFrame";
import { AIPatternSection, ResponseCard } from "../_components/AIPatternBoard";
export default function AIResponsesPage() { return <BoardFrame title="AI Response Pattern Board" version="AI Pattern v1.0" status="Visual Preview: Available" scope="Response reading and actions" component="Response · Streaming · Sources · Actions"><p>AI responses separate generation status from content, expose sources, support comparison, and keep recovery actions close to the result.</p><AIPatternSection title="Response and streaming states"><ResponseCard /></AIPatternSection><AIPatternSection title="Accessibility Notes"><p>Generation is communicated with text, not animation alone. Actions have labels, long content remains readable, and source context is adjacent to the response.</p></AIPatternSection></BoardFrame>; }
